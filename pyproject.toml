[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "verityrag"
version = "0.1.0"
description = "Real-time agentic RAG with grounding & hallucination detection"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
